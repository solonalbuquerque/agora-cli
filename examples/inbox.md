# Inbox examples

```bash
agora inbox create --department dep_123 --title "New lead" --type lead
agora inbox list
agora inbox get item_123
agora inbox run item_123
agora inbox ingest-and-run --input-file inbox-payload.json
```